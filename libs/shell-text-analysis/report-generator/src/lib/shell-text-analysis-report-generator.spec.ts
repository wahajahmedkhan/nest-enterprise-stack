import { shellTextAnalysisReportGenerator } from './shell-text-analysis-report-generator';

describe('shellTextAnalysisReportGenerator', () => {
  it('should work', () => {
    expect(shellTextAnalysisReportGenerator()).toEqual(
      'shell-text-analysis-report-generator',
    );
  });
});
